export enum NotePatchOptions {
  CANCEL = "cancel",
  SAVE = "save"
}

export enum DbTabs {
  ADD = "add",
  LOAD = "load"
}

export enum ViewTabs {
  CODES = "codes",
  TAXES = "taxes",
  TRANSACTIONS = "transactions",
  VENDORS = "vendors"
}

export enum UpdateToggles {
  CODE = "code",
  LABEL = "label",
  VENDOR = "vendor"
}
