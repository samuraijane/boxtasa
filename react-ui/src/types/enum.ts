export enum NotePatchOptions {
  CANCEL = "cancel",
  SAVE = "save"
}

export enum DbTabs {
  ADD = "add",
  LOAD = "load"
}

export enum ViewTabs {
  CODES = "codes",
  TRANSACTIONS = "transactions",
  VENDORS = "vendors"
}
