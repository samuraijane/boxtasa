{
  "name": "boxtasa",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "cd react-ui/ && npm install && npm run build",
    "db:export": "sh export.sh",
    "db:run": "psql -U sj -d boxtasa -f ./sql-temp/run.sql",
    "dev": "export NODE_ENV=dev && nodemon .",
    "start": "export NODE_ENV=prod && node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "pg": "^8.11.3"
  }
}
